apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: olss-demo-app
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: olss-demo-app
                port:
                 number: 8080
